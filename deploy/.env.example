# Copy to deploy/.env and fill in. .env is gitignored.

FTP_HOST=ftp.example.com
FTP_USER=username
FTP_PASS=password
FTP_REMOTE_DIR=/var/www/xive

# optional
# FTP_PORT=21
# FTP_PROTOCOL=ftp        # or "ftps"
# SKIP_BUILD=0            # set to 1 to deploy current dist/ without rebuilding
